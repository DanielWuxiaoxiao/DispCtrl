# 第9章 测试策略与性能评估

## 单元测试
- 建议对 Manager 层解析/缓存逻辑编写单元测试（使用轻量测试框架，tests/）。

## 集成测试
- 使用脚本模拟 UDP 报文流，包括高并发场景、异常报文、丢包场景。
- 检查 GUI 反应时间、内存使用、帧率与资源释放。

## 性能指标
- RTT（recv -> render）应在可接受范围内（根据硬件目标）。
- 建议记录并展示关键性能统计（每分钟/每小时）。

## 回归测试
- 编写自动化测试套件验证：协议字段变化、BIT 显示、对话恢复参数、地图中心同步。